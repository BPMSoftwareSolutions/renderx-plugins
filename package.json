{
  "name": "renderx-plugins",
  "version": "2.0.5",
  "public": true,
  "type": "module",
  "files": [
    "dist/artifacts/**/*",
    "dist/types.d.ts"
  ],
  "main": "dist/artifacts/manifest-set.json",
  "exports": {
    ".": "./dist/artifacts/manifest-set.json",
    "./plugin-manifest": "./dist/artifacts/plugins/plugin-manifest.json"
  },
  "types": "./dist/types.d.ts",
  "scripts": {
    "build:packages": "node scripts/build-packages.cjs",
    "post-fix:manifest": "node scripts/post-fix-manifest.cjs",
    "artifacts:build": "npm run build:packages && npm run build:plugin-entries && node scripts/build-artifacts.js --srcRoot=. --outDir=dist/artifacts --integrity && npm run post-fix:manifest",
    "artifacts:build:signed": "npm run build:packages && npm run build:plugin-entries && node scripts/build-artifacts.js --srcRoot=. --outDir=dist/artifacts --integrity --sign && npm run post-fix:manifest",
    "artifacts:validate": "node scripts/validate-artifacts.js",
    "artifacts:validate:strict": "cross-env RENDERX_VALIDATION_STRICT=1 node scripts/validate-artifacts.js",
    "artifacts:verify:signature": "node scripts/verify-artifact-signature.js",
    "artifacts:pack": "node scripts/pack-artifacts.js --dir=dist/artifacts --out=dist/packages",
    "public-api:hash": "node scripts/hash-public-api.js",
    "public-api:check": "node scripts/hash-public-api.js --check",
    "artifacts:ci": "npm run artifacts:build:signed && npm run artifacts:validate:strict && npm run artifacts:verify:signature && npm run public-api:check && npm run artifacts:pack",
    "build:plugin-entries": "node scripts/build-plugin-entries.cjs",
    "build": "npm run build:packages && npm run build:plugin-entries",
    "prepare": "npm run artifacts:build"
  },
  "devDependencies": {
    "@renderx/manifest-tools": "file:packages/manifest-tools",
    "@renderx/schema-contract": "file:packages/schema-contract",
    "cross-env": "^10.0.0",
    "esbuild": "^0.25.9",
    "eslint": "^9.33.0"
  }
}
